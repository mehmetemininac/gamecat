<% title "Moviegeneres" %>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>
  <% for moviegenere in @moviegeneres %>
    <tr>
      <td><%=h moviegenere.name %></td>
      <td><%=h moviegenere.description %></td>
      <%= common_links :section => :list,:object =>  moviegenere %>
    </tr>
  <% end %>
</table>
<%= common_links :section => :main, :klass => Moviegenere %>
